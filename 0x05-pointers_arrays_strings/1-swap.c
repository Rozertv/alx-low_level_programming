#include "main.h"

/**
 * swap_int - take in two variable integers and swap them
 * @authorv Anisah
 * @a: swaps and stores adresse of b
 * @b: swaps and stores adress of a
 * Return: 0
 */

void swap_int(int *a, int *b)
{
int swap;
change = *b;
*b = *a;
*a = change;
}
